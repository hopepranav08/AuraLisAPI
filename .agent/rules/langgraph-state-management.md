---
trigger: always_on
---

LangGraph workflows must use TypedDict state definitions.

State updates must use Annotated reducers.

Use operator.add for list aggregation.

Do not overwrite existing state values.

Graph must support persistence and resumability.

Human-in-the-loop checkpoints must exist before enforcement actions.